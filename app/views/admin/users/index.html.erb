<h1>Listing users</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Name</th>
    <th>User role</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.name %></td>
    <td><%= "#{user.user_role.role} - #{user.user_role.description}" %></td>
	<% #if has_owner_privileges? %>
		<% # collection_select(:user, :course_id, current_user.courses, :id, :name) %>
	<% # end %>
    <td><%= link_to 'Show',[:admin, user] %></td>
	
  </tr>
<% end %>
</table>

<br />
